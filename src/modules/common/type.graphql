scalar Email
scalar Date
scalar Upload

directive @auth(requires: Role = Admin) on OBJECT | FIELD_DEFINITION

enum Role {
  Admin
  Moderator
  User
}

type Query {
  _: String
}
type Mutation {
  _: String
  imageUploader(file: Upload!): String!
}
type Subscription {
  _: String
}
